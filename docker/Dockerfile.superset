FROM apache/superset:latest

USER root

# Install PostgreSQL driver system-wide and in virtual env
RUN pip install psycopg2-binary && \
    if [ -d /app/.venv ]; then /app/.venv/bin/pip install psycopg2-binary || true; fi

USER superset
